CONSULTA CREACIÓN CLIENTE

BEGIN
    INSERT INTO CLIENT (ID, NAME, EMAIL, AGE) VALUES(:id, :name, :email, :age);
    :status_code := 201;
END;

CONSULTA GET CLIENTES

SELECT * FROM CLIENTE

CONSULTA PUT CLIENT

BEGIN
    UPDATE CLIENT SET NAME = :name, EMAIL = :email, AGE = :age WHERE ID = :id;
    :status_code := 201;
END;


CONSULTA DELETE CLIENT
BEGIN
    DELETE FROM CLIENT WHERE ID = :id;
    :status_code := 204;
END;

CONSULTA GET CLIENTE ESPECIFICO
SELECT * FROM CLIENT WHERE ID = :id